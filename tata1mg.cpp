#include <iostream>
#include <vector> 

using namespace std;
int func(vector<int>& dp,vector<int>& v,int ind)
{
    int skip=ind+1;
    int ans=INT_MAX;
    ans=min//pick 
}
int main()
{
    int n; cin>>n;
    vector<int> v(n);
    for(int i=0;i<n;i++)
    {
       cin>>v[i];
    }
    vector<int> dp(n,-1);
    dp[0]=func(dp,v,0);
}
